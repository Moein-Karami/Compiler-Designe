=begin yet another secret sample!
in two lines! =end

class Sample
    private int var(bool k = true, int[12] g = 12) {
        if k
            if a * g
                return app(a, b)
            elsif anotherCond + !-this
                    size(f && r)



        else
                if (true * !true)
                    print(a = b * s)
                    else {
                        print(append(a * v, n / d));
                        print(a.g + a())
                        }
}

class Main {
    private int f() {
            int jj, tt, f, d, k;
            # what??
            i = b > f || ! fj * d * s * (d ==s ) ? i + 1 : f * g - f + g
    }
    private void f2() {
        int i
        (0..j*r).each do |i| {
            if a {
                return 2;
                 return 3;
            }
            return 4 * 4;
             if b
            return 3;
             print(func)
            if k
                if t
                    size(t)
                else
                    app(t, t)
            if kk
                if tt
                    return 44;
                     else
                        return 3*j
        }

    }


    public initialize() {
            return 10;
             if cond
                print(true * false);
                return 3;

    }

}


